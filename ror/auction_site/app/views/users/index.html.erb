<h1>Users</h1>
<ul>
	<% @users.each do |user| %>
		<li>
			<%= link_to(user.name, user_path(user)) %>
		</li>
	<% end %>
</ul>
<div>
<%= link_to('Add new user', new_user_path) %>
<br>
<%= link_to('Back home', '/') %>
</div>